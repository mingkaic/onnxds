workspace(name = "com_github_mingkaic_onnxds")

# === load local dependencies ===

load("//third_party:all.bzl", onnx_ds_deps="dependencies")
onnx_ds_deps()

# === load protobuf dependencies ===

load("@com_google_protobuf//:protobuf_deps.bzl", "protobuf_deps")
protobuf_deps()
